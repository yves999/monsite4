import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-nadal',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './nadal.html',
  styleUrl: './nadal.css',
})
export class Nadal {}
